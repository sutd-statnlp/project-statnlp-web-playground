(function () {
    'use strict';
    angular
        .module('visualApp')
        .factory('DialogService', DialogService);

    DialogService.$inject = ['$mdDialog'];

    function DialogService($mdDialog) {
        var service = {
           
        };
        return service;


    }
})();
